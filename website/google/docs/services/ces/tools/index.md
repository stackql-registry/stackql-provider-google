--- 
title: tools
hide_title: false
hide_table_of_contents: false
keywords:
  - tools
  - ces
  - google
  - infrastructure-as-code
  - configuration-as-data
  - cloud inventory
description: Query, deploy and manage google resources using SQL
custom_edit_url: null
image: /img/stackql-google-provider-featured-image.png
---

import CopyableCode from '@site/src/components/CopyableCode/CopyableCode';
import CodeBlock from '@theme/CodeBlock';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

Creates, updates, deletes, gets or lists a <code>tools</code> resource.

## Overview
<table><tbody>
<tr><td><b>Name</b></td><td><CopyableCode code="tools" /></td></tr>
<tr><td><b>Type</b></td><td>Resource</td></tr>
<tr><td><b>Id</b></td><td><CopyableCode code="google.ces.tools" /></td></tr>
</tbody></table>

## Fields

The following fields are returned by `SELECT` queries:

<Tabs
    defaultValue="get"
    values={[
        { label: 'get', value: 'get' },
        { label: 'list', value: 'list' }
    ]}
>
<TabItem value="get">

<table>
<thead>
    <tr>
    <th>Name</th>
    <th>Datatype</th>
    <th>Description</th>
    </tr>
</thead>
<tbody>
<tr>
    <td><CopyableCode code="name" /></td>
    <td><code>string</code></td>
    <td>Identifier. The resource name of the tool. Format: * `projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/tools/&#123;tool&#125;` for standalone tools. * `projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/toolsets/&#123;toolset&#125;/tools/&#123;tool&#125;` for tools retrieved from a toolset. These tools are dynamic and output-only; they cannot be referenced directly where a tool is expected.</td>
</tr>
<tr>
    <td><CopyableCode code="agentTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The agent tool. (id: AgentTool)</td>
</tr>
<tr>
    <td><CopyableCode code="clientFunction" /></td>
    <td><code>object</code></td>
    <td>Optional. The client function. (id: ClientFunction)</td>
</tr>
<tr>
    <td><CopyableCode code="connectorTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The Integration Connector tool. (id: ConnectorTool)</td>
</tr>
<tr>
    <td><CopyableCode code="createTime" /></td>
    <td><code>string (google-datetime)</code></td>
    <td>Output only. Timestamp when the tool was created.</td>
</tr>
<tr>
    <td><CopyableCode code="dataStoreTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The data store tool. (id: DataStoreTool)</td>
</tr>
<tr>
    <td><CopyableCode code="displayName" /></td>
    <td><code>string</code></td>
    <td>Output only. The display name of the tool, derived based on the tool's type. For example, display name of a ClientFunction is derived from its `name` property.</td>
</tr>
<tr>
    <td><CopyableCode code="etag" /></td>
    <td><code>string</code></td>
    <td>Etag used to ensure the object hasn't changed during a read-modify-write operation. If the etag is empty, the update will overwrite any concurrent changes.</td>
</tr>
<tr>
    <td><CopyableCode code="executionType" /></td>
    <td><code>string</code></td>
    <td>Optional. The execution type of the tool. (EXECUTION_TYPE_UNSPECIFIED, SYNCHRONOUS, ASYNCHRONOUS)</td>
</tr>
<tr>
    <td><CopyableCode code="fileSearchTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The file search tool. (id: FileSearchTool)</td>
</tr>
<tr>
    <td><CopyableCode code="generatedSummary" /></td>
    <td><code>string</code></td>
    <td>Output only. If the tool is generated by the LLM assistant, this field contains a descriptive summary of the generation.</td>
</tr>
<tr>
    <td><CopyableCode code="googleSearchTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The google search tool. (id: GoogleSearchTool)</td>
</tr>
<tr>
    <td><CopyableCode code="mcpTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The MCP tool. An MCP tool cannot be created or updated directly and is managed by the MCP toolset. (id: McpTool)</td>
</tr>
<tr>
    <td><CopyableCode code="openApiTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The open API tool. (id: OpenApiTool)</td>
</tr>
<tr>
    <td><CopyableCode code="pythonFunction" /></td>
    <td><code>object</code></td>
    <td>Optional. The python function tool. (id: PythonFunction)</td>
</tr>
<tr>
    <td><CopyableCode code="remoteAgentTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The remote agent tool. (id: RemoteAgentTool)</td>
</tr>
<tr>
    <td><CopyableCode code="systemTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The system tool. (id: SystemTool)</td>
</tr>
<tr>
    <td><CopyableCode code="timeout" /></td>
    <td><code>string (google-duration)</code></td>
    <td>Optional. The timeout for the tool execution. If not set, the default timeout is 30 seconds for `SYNCHRONOUS` tools and 60 seconds for `ASYNCHRONOUS` tools.</td>
</tr>
<tr>
    <td><CopyableCode code="toolFakeConfig" /></td>
    <td><code>object</code></td>
    <td>Optional. Configuration for tool behavior in fake mode. (id: ToolFakeConfig)</td>
</tr>
<tr>
    <td><CopyableCode code="updateTime" /></td>
    <td><code>string (google-datetime)</code></td>
    <td>Output only. Timestamp when the tool was last updated.</td>
</tr>
<tr>
    <td><CopyableCode code="widgetTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The widget tool. (id: WidgetTool)</td>
</tr>
</tbody>
</table>
</TabItem>
<TabItem value="list">

<table>
<thead>
    <tr>
    <th>Name</th>
    <th>Datatype</th>
    <th>Description</th>
    </tr>
</thead>
<tbody>
<tr>
    <td><CopyableCode code="name" /></td>
    <td><code>string</code></td>
    <td>Identifier. The resource name of the tool. Format: * `projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/tools/&#123;tool&#125;` for standalone tools. * `projects/&#123;project&#125;/locations/&#123;location&#125;/apps/&#123;app&#125;/toolsets/&#123;toolset&#125;/tools/&#123;tool&#125;` for tools retrieved from a toolset. These tools are dynamic and output-only; they cannot be referenced directly where a tool is expected.</td>
</tr>
<tr>
    <td><CopyableCode code="agentTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The agent tool. (id: AgentTool)</td>
</tr>
<tr>
    <td><CopyableCode code="clientFunction" /></td>
    <td><code>object</code></td>
    <td>Optional. The client function. (id: ClientFunction)</td>
</tr>
<tr>
    <td><CopyableCode code="connectorTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The Integration Connector tool. (id: ConnectorTool)</td>
</tr>
<tr>
    <td><CopyableCode code="createTime" /></td>
    <td><code>string (google-datetime)</code></td>
    <td>Output only. Timestamp when the tool was created.</td>
</tr>
<tr>
    <td><CopyableCode code="dataStoreTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The data store tool. (id: DataStoreTool)</td>
</tr>
<tr>
    <td><CopyableCode code="displayName" /></td>
    <td><code>string</code></td>
    <td>Output only. The display name of the tool, derived based on the tool's type. For example, display name of a ClientFunction is derived from its `name` property.</td>
</tr>
<tr>
    <td><CopyableCode code="etag" /></td>
    <td><code>string</code></td>
    <td>Etag used to ensure the object hasn't changed during a read-modify-write operation. If the etag is empty, the update will overwrite any concurrent changes.</td>
</tr>
<tr>
    <td><CopyableCode code="executionType" /></td>
    <td><code>string</code></td>
    <td>Optional. The execution type of the tool. (EXECUTION_TYPE_UNSPECIFIED, SYNCHRONOUS, ASYNCHRONOUS)</td>
</tr>
<tr>
    <td><CopyableCode code="fileSearchTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The file search tool. (id: FileSearchTool)</td>
</tr>
<tr>
    <td><CopyableCode code="generatedSummary" /></td>
    <td><code>string</code></td>
    <td>Output only. If the tool is generated by the LLM assistant, this field contains a descriptive summary of the generation.</td>
</tr>
<tr>
    <td><CopyableCode code="googleSearchTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The google search tool. (id: GoogleSearchTool)</td>
</tr>
<tr>
    <td><CopyableCode code="mcpTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The MCP tool. An MCP tool cannot be created or updated directly and is managed by the MCP toolset. (id: McpTool)</td>
</tr>
<tr>
    <td><CopyableCode code="openApiTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The open API tool. (id: OpenApiTool)</td>
</tr>
<tr>
    <td><CopyableCode code="pythonFunction" /></td>
    <td><code>object</code></td>
    <td>Optional. The python function tool. (id: PythonFunction)</td>
</tr>
<tr>
    <td><CopyableCode code="remoteAgentTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The remote agent tool. (id: RemoteAgentTool)</td>
</tr>
<tr>
    <td><CopyableCode code="systemTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The system tool. (id: SystemTool)</td>
</tr>
<tr>
    <td><CopyableCode code="timeout" /></td>
    <td><code>string (google-duration)</code></td>
    <td>Optional. The timeout for the tool execution. If not set, the default timeout is 30 seconds for `SYNCHRONOUS` tools and 60 seconds for `ASYNCHRONOUS` tools.</td>
</tr>
<tr>
    <td><CopyableCode code="toolFakeConfig" /></td>
    <td><code>object</code></td>
    <td>Optional. Configuration for tool behavior in fake mode. (id: ToolFakeConfig)</td>
</tr>
<tr>
    <td><CopyableCode code="updateTime" /></td>
    <td><code>string (google-datetime)</code></td>
    <td>Output only. Timestamp when the tool was last updated.</td>
</tr>
<tr>
    <td><CopyableCode code="widgetTool" /></td>
    <td><code>object</code></td>
    <td>Optional. The widget tool. (id: WidgetTool)</td>
</tr>
</tbody>
</table>
</TabItem>
</Tabs>

## Methods

The following methods are available for this resource:

<table>
<thead>
    <tr>
    <th>Name</th>
    <th>Accessible by</th>
    <th>Required Params</th>
    <th>Optional Params</th>
    <th>Description</th>
    </tr>
</thead>
<tbody>
<tr>
    <td><a href="#get"><CopyableCode code="get" /></a></td>
    <td><CopyableCode code="select" /></td>
    <td><a href="#parameter-projectsId"><code>projectsId</code></a>, <a href="#parameter-locationsId"><code>locationsId</code></a>, <a href="#parameter-appsId"><code>appsId</code></a>, <a href="#parameter-toolsId"><code>toolsId</code></a></td>
    <td></td>
    <td>Gets details of the specified tool.</td>
</tr>
<tr>
    <td><a href="#list"><CopyableCode code="list" /></a></td>
    <td><CopyableCode code="select" /></td>
    <td><a href="#parameter-projectsId"><code>projectsId</code></a>, <a href="#parameter-locationsId"><code>locationsId</code></a>, <a href="#parameter-appsId"><code>appsId</code></a></td>
    <td><a href="#parameter-pageSize"><code>pageSize</code></a>, <a href="#parameter-pageToken"><code>pageToken</code></a>, <a href="#parameter-filter"><code>filter</code></a>, <a href="#parameter-orderBy"><code>orderBy</code></a></td>
    <td>Lists tools in the given app.</td>
</tr>
<tr>
    <td><a href="#create"><CopyableCode code="create" /></a></td>
    <td><CopyableCode code="insert" /></td>
    <td><a href="#parameter-projectsId"><code>projectsId</code></a>, <a href="#parameter-locationsId"><code>locationsId</code></a>, <a href="#parameter-appsId"><code>appsId</code></a></td>
    <td><a href="#parameter-toolId"><code>toolId</code></a></td>
    <td>Creates a new tool in the given app.</td>
</tr>
<tr>
    <td><a href="#patch"><CopyableCode code="patch" /></a></td>
    <td><CopyableCode code="update" /></td>
    <td><a href="#parameter-projectsId"><code>projectsId</code></a>, <a href="#parameter-locationsId"><code>locationsId</code></a>, <a href="#parameter-appsId"><code>appsId</code></a>, <a href="#parameter-toolsId"><code>toolsId</code></a></td>
    <td><a href="#parameter-updateMask"><code>updateMask</code></a></td>
    <td>Updates the specified tool.</td>
</tr>
<tr>
    <td><a href="#delete"><CopyableCode code="delete" /></a></td>
    <td><CopyableCode code="delete" /></td>
    <td><a href="#parameter-projectsId"><code>projectsId</code></a>, <a href="#parameter-locationsId"><code>locationsId</code></a>, <a href="#parameter-appsId"><code>appsId</code></a>, <a href="#parameter-toolsId"><code>toolsId</code></a></td>
    <td><a href="#parameter-force"><code>force</code></a>, <a href="#parameter-etag"><code>etag</code></a></td>
    <td>Deletes the specified tool.</td>
</tr>
</tbody>
</table>

## Parameters

Parameters can be passed in the `WHERE` clause of a query. Check the [Methods](#methods) section to see which parameters are required or optional for each operation.

<table>
<thead>
    <tr>
    <th>Name</th>
    <th>Datatype</th>
    <th>Description</th>
    </tr>
</thead>
<tbody>
<tr id="parameter-appsId">
    <td><CopyableCode code="appsId" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-locationsId">
    <td><CopyableCode code="locationsId" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-projectsId">
    <td><CopyableCode code="projectsId" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-toolsId">
    <td><CopyableCode code="toolsId" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-etag">
    <td><CopyableCode code="etag" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-filter">
    <td><CopyableCode code="filter" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-force">
    <td><CopyableCode code="force" /></td>
    <td><code>boolean</code></td>
    <td></td>
</tr>
<tr id="parameter-orderBy">
    <td><CopyableCode code="orderBy" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-pageSize">
    <td><CopyableCode code="pageSize" /></td>
    <td><code>integer (int32)</code></td>
    <td></td>
</tr>
<tr id="parameter-pageToken">
    <td><CopyableCode code="pageToken" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-toolId">
    <td><CopyableCode code="toolId" /></td>
    <td><code>string</code></td>
    <td></td>
</tr>
<tr id="parameter-updateMask">
    <td><CopyableCode code="updateMask" /></td>
    <td><code>string (google-fieldmask)</code></td>
    <td></td>
</tr>
</tbody>
</table>

## `SELECT` examples

<Tabs
    defaultValue="get"
    values={[
        { label: 'get', value: 'get' },
        { label: 'list', value: 'list' }
    ]}
>
<TabItem value="get">

Gets details of the specified tool.

```sql
SELECT
name,
agentTool,
clientFunction,
connectorTool,
createTime,
dataStoreTool,
displayName,
etag,
executionType,
fileSearchTool,
generatedSummary,
googleSearchTool,
mcpTool,
openApiTool,
pythonFunction,
remoteAgentTool,
systemTool,
timeout,
toolFakeConfig,
updateTime,
widgetTool
FROM google.ces.tools
WHERE projectsId = '{{ projectsId }}' -- required
AND locationsId = '{{ locationsId }}' -- required
AND appsId = '{{ appsId }}' -- required
AND toolsId = '{{ toolsId }}' -- required
;
```
</TabItem>
<TabItem value="list">

Lists tools in the given app.

```sql
SELECT
name,
agentTool,
clientFunction,
connectorTool,
createTime,
dataStoreTool,
displayName,
etag,
executionType,
fileSearchTool,
generatedSummary,
googleSearchTool,
mcpTool,
openApiTool,
pythonFunction,
remoteAgentTool,
systemTool,
timeout,
toolFakeConfig,
updateTime,
widgetTool
FROM google.ces.tools
WHERE projectsId = '{{ projectsId }}' -- required
AND locationsId = '{{ locationsId }}' -- required
AND appsId = '{{ appsId }}' -- required
AND pageSize = '{{ pageSize }}'
AND pageToken = '{{ pageToken }}'
AND filter = '{{ filter }}'
AND orderBy = '{{ orderBy }}'
;
```
</TabItem>
</Tabs>


## `INSERT` examples

<Tabs
    defaultValue="create"
    values={[
        { label: 'create', value: 'create' },
        { label: 'Manifest', value: 'manifest' }
    ]}
>
<TabItem value="create">

Creates a new tool in the given app.

```sql
INSERT INTO google.ces.tools (
data__connectorTool,
data__timeout,
data__name,
data__openApiTool,
data__fileSearchTool,
data__systemTool,
data__widgetTool,
data__pythonFunction,
data__mcpTool,
data__googleSearchTool,
data__clientFunction,
data__remoteAgentTool,
data__dataStoreTool,
data__etag,
data__executionType,
data__agentTool,
data__toolFakeConfig,
projectsId,
locationsId,
appsId,
toolId
)
SELECT 
'{{ connectorTool }}',
'{{ timeout }}',
'{{ name }}',
'{{ openApiTool }}',
'{{ fileSearchTool }}',
'{{ systemTool }}',
'{{ widgetTool }}',
'{{ pythonFunction }}',
'{{ mcpTool }}',
'{{ googleSearchTool }}',
'{{ clientFunction }}',
'{{ remoteAgentTool }}',
'{{ dataStoreTool }}',
'{{ etag }}',
'{{ executionType }}',
'{{ agentTool }}',
'{{ toolFakeConfig }}',
'{{ projectsId }}',
'{{ locationsId }}',
'{{ appsId }}',
'{{ toolId }}'
RETURNING
name,
agentTool,
clientFunction,
connectorTool,
createTime,
dataStoreTool,
displayName,
etag,
executionType,
fileSearchTool,
generatedSummary,
googleSearchTool,
mcpTool,
openApiTool,
pythonFunction,
remoteAgentTool,
systemTool,
timeout,
toolFakeConfig,
updateTime,
widgetTool
;
```
</TabItem>
<TabItem value="manifest">

<CodeBlock language="yaml">{`# Description fields are for documentation purposes
- name: tools
  props:
    - name: projectsId
      value: "{{ projectsId }}"
      description: Required parameter for the tools resource.
    - name: locationsId
      value: "{{ locationsId }}"
      description: Required parameter for the tools resource.
    - name: appsId
      value: "{{ appsId }}"
      description: Required parameter for the tools resource.
    - name: connectorTool
      description: |
        Optional. The Integration Connector tool.
      value:
        action:
          inputFields:
            - "{{ inputFields }}"
          connectionActionId: "{{ connectionActionId }}"
          entityOperation:
            operation: "{{ operation }}"
            entityId: "{{ entityId }}"
          outputFields:
            - "{{ outputFields }}"
        description: "{{ description }}"
        name: "{{ name }}"
        connection: "{{ connection }}"
        authConfig:
          oauth2AuthCodeConfig:
            oauthToken: "{{ oauthToken }}"
          oauth2JwtBearerConfig:
            issuer: "{{ issuer }}"
            subject: "{{ subject }}"
            clientKey: "{{ clientKey }}"
    - name: timeout
      value: "{{ timeout }}"
      description: |
        Optional. The timeout for the tool execution. If not set, the default timeout is 30 seconds for \`SYNCHRONOUS\` tools and 60 seconds for \`ASYNCHRONOUS\` tools.
    - name: name
      value: "{{ name }}"
      description: |
        Identifier. The resource name of the tool. Format: * \`projects/{project}/locations/{location}/apps/{app}/tools/{tool}\` for standalone tools. * \`projects/{project}/locations/{location}/apps/{app}/toolsets/{toolset}/tools/{tool}\` for tools retrieved from a toolset. These tools are dynamic and output-only; they cannot be referenced directly where a tool is expected.
    - name: openApiTool
      description: |
        Optional. The open API tool.
      value:
        apiAuthentication:
          bearerTokenConfig:
            token: "{{ token }}"
          serviceAccountAuthConfig:
            serviceAccount: "{{ serviceAccount }}"
            scopes:
              - "{{ scopes }}"
          apiKeyConfig:
            keyName: "{{ keyName }}"
            requestLocation: "{{ requestLocation }}"
            apiKeySecretVersion: "{{ apiKeySecretVersion }}"
          oauthConfig:
            tokenEndpoint: "{{ tokenEndpoint }}"
            oauthGrantType: "{{ oauthGrantType }}"
            clientId: "{{ clientId }}"
            clientSecretVersion: "{{ clientSecretVersion }}"
            scopes:
              - "{{ scopes }}"
          serviceAgentIdTokenAuthConfig: "{{ serviceAgentIdTokenAuthConfig }}"
        url: "{{ url }}"
        openApiSchema: "{{ openApiSchema }}"
        ignoreUnknownFields: {{ ignoreUnknownFields }}
        name: "{{ name }}"
        description: "{{ description }}"
        tlsConfig:
          caCerts:
            - displayName: "{{ displayName }}"
              cert: "{{ cert }}"
        serviceDirectoryConfig:
          service: "{{ service }}"
    - name: fileSearchTool
      description: |
        Optional. The file search tool.
      value:
        corpusType: "{{ corpusType }}"
        name: "{{ name }}"
        fileCorpus: "{{ fileCorpus }}"
        description: "{{ description }}"
    - name: systemTool
      description: |
        Optional. The system tool.
      value:
        name: "{{ name }}"
        description: "{{ description }}"
    - name: widgetTool
      description: |
        Optional. The widget tool.
      value:
        uiConfig: "{{ uiConfig }}"
        widgetType: "{{ widgetType }}"
        dataMapping:
          fieldMappings: "{{ fieldMappings }}"
          mode: "{{ mode }}"
          pythonFunction:
            description: "{{ description }}"
            name: "{{ name }}"
            pythonCode: "{{ pythonCode }}"
            serviceDirectoryConfig:
              service: "{{ service }}"
          sourceToolName: "{{ sourceToolName }}"
          pythonScript: "{{ pythonScript }}"
        name: "{{ name }}"
        parameters:
          type: "{{ type }}"
          minimum: {{ minimum }}
          items:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          nullable: {{ nullable }}
          maximum: {{ maximum }}
          description: "{{ description }}"
          default: "{{ default }}"
          ref: "{{ ref }}"
          minItems: "{{ minItems }}"
          anyOf:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          enum:
            - "{{ enum }}"
          required:
            - "{{ required }}"
          properties: "{{ properties }}"
          prefixItems:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          defs: "{{ defs }}"
          maxItems: "{{ maxItems }}"
          uniqueItems: {{ uniqueItems }}
          additionalProperties:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          title: "{{ title }}"
        description: "{{ description }}"
        textResponseConfig:
          type: "{{ type }}"
          staticText: "{{ staticText }}"
          textResponseInstruction: "{{ textResponseInstruction }}"
    - name: pythonFunction
      description: |
        Optional. The python function tool.
      value:
        description: "{{ description }}"
        name: "{{ name }}"
        pythonCode: "{{ pythonCode }}"
        serviceDirectoryConfig:
          service: "{{ service }}"
    - name: mcpTool
      description: |
        Optional. The MCP tool. An MCP tool cannot be created or updated directly and is managed by the MCP toolset.
      value:
        description: "{{ description }}"
        customHeaders: "{{ customHeaders }}"
        serviceDirectoryConfig:
          service: "{{ service }}"
        inputSchema:
          type: "{{ type }}"
          minimum: {{ minimum }}
          items:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          nullable: {{ nullable }}
          maximum: {{ maximum }}
          description: "{{ description }}"
          default: "{{ default }}"
          ref: "{{ ref }}"
          minItems: "{{ minItems }}"
          anyOf:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          enum:
            - "{{ enum }}"
          required:
            - "{{ required }}"
          properties: "{{ properties }}"
          prefixItems:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          defs: "{{ defs }}"
          maxItems: "{{ maxItems }}"
          uniqueItems: {{ uniqueItems }}
          additionalProperties:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          title: "{{ title }}"
        name: "{{ name }}"
        nameOverride: "{{ nameOverride }}"
        outputSchema:
          type: "{{ type }}"
          minimum: {{ minimum }}
          items:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          nullable: {{ nullable }}
          maximum: {{ maximum }}
          description: "{{ description }}"
          default: "{{ default }}"
          ref: "{{ ref }}"
          minItems: "{{ minItems }}"
          anyOf:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          enum:
            - "{{ enum }}"
          required:
            - "{{ required }}"
          properties: "{{ properties }}"
          prefixItems:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          defs: "{{ defs }}"
          maxItems: "{{ maxItems }}"
          uniqueItems: {{ uniqueItems }}
          additionalProperties:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          title: "{{ title }}"
        serverAddress: "{{ serverAddress }}"
        state: "{{ state }}"
        tlsConfig:
          caCerts:
            - displayName: "{{ displayName }}"
              cert: "{{ cert }}"
        apiAuthentication:
          bearerTokenConfig:
            token: "{{ token }}"
          serviceAccountAuthConfig:
            serviceAccount: "{{ serviceAccount }}"
            scopes:
              - "{{ scopes }}"
          apiKeyConfig:
            keyName: "{{ keyName }}"
            requestLocation: "{{ requestLocation }}"
            apiKeySecretVersion: "{{ apiKeySecretVersion }}"
          oauthConfig:
            tokenEndpoint: "{{ tokenEndpoint }}"
            oauthGrantType: "{{ oauthGrantType }}"
            clientId: "{{ clientId }}"
            clientSecretVersion: "{{ clientSecretVersion }}"
            scopes:
              - "{{ scopes }}"
          serviceAgentIdTokenAuthConfig: "{{ serviceAgentIdTokenAuthConfig }}"
    - name: googleSearchTool
      description: |
        Optional. The google search tool.
      value:
        name: "{{ name }}"
        promptConfig:
          textPrompt: "{{ textPrompt }}"
          voicePrompt: "{{ voicePrompt }}"
        preferredDomains:
          - "{{ preferredDomains }}"
        excludeDomains:
          - "{{ excludeDomains }}"
        description: "{{ description }}"
        contextUrls:
          - "{{ contextUrls }}"
    - name: clientFunction
      description: |
        Optional. The client function.
      value:
        name: "{{ name }}"
        response:
          type: "{{ type }}"
          minimum: {{ minimum }}
          items:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          nullable: {{ nullable }}
          maximum: {{ maximum }}
          description: "{{ description }}"
          default: "{{ default }}"
          ref: "{{ ref }}"
          minItems: "{{ minItems }}"
          anyOf:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          enum:
            - "{{ enum }}"
          required:
            - "{{ required }}"
          properties: "{{ properties }}"
          prefixItems:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          defs: "{{ defs }}"
          maxItems: "{{ maxItems }}"
          uniqueItems: {{ uniqueItems }}
          additionalProperties:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          title: "{{ title }}"
        description: "{{ description }}"
        parameters:
          type: "{{ type }}"
          minimum: {{ minimum }}
          items:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          nullable: {{ nullable }}
          maximum: {{ maximum }}
          description: "{{ description }}"
          default: "{{ default }}"
          ref: "{{ ref }}"
          minItems: "{{ minItems }}"
          anyOf:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          enum:
            - "{{ enum }}"
          required:
            - "{{ required }}"
          properties: "{{ properties }}"
          prefixItems:
            - type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf: "{{ anyOf }}"
              enum: "{{ enum }}"
              required: "{{ required }}"
              properties: "{{ properties }}"
              prefixItems: "{{ prefixItems }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                enum:
                  - "{{ enum }}"
                required:
                  - "{{ required }}"
                properties: "{{ properties }}"
                prefixItems:
                  - type: "{{ type }}"
                    minimum: {{ minimum }}
                    items:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties:
                      type: "{{ type }}"
                      minimum: {{ minimum }}
                      items: "{{ items }}"
                      nullable: {{ nullable }}
                      maximum: {{ maximum }}
                      description: "{{ description }}"
                      default: "{{ default }}"
                      ref: "{{ ref }}"
                      minItems: "{{ minItems }}"
                      anyOf: "{{ anyOf }}"
                      enum: "{{ enum }}"
                      required: "{{ required }}"
                      properties: "{{ properties }}"
                      prefixItems: "{{ prefixItems }}"
                      defs: "{{ defs }}"
                      maxItems: "{{ maxItems }}"
                      uniqueItems: {{ uniqueItems }}
                      additionalProperties: "{{ additionalProperties }}"
                      title: "{{ title }}"
                    title: "{{ title }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
              title: "{{ title }}"
          defs: "{{ defs }}"
          maxItems: "{{ maxItems }}"
          uniqueItems: {{ uniqueItems }}
          additionalProperties:
            type: "{{ type }}"
            minimum: {{ minimum }}
            items:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            nullable: {{ nullable }}
            maximum: {{ maximum }}
            description: "{{ description }}"
            default: "{{ default }}"
            ref: "{{ ref }}"
            minItems: "{{ minItems }}"
            anyOf:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            enum:
              - "{{ enum }}"
            required:
              - "{{ required }}"
            properties: "{{ properties }}"
            prefixItems:
              - type: "{{ type }}"
                minimum: {{ minimum }}
                items:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties:
                  type: "{{ type }}"
                  minimum: {{ minimum }}
                  items: "{{ items }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties: "{{ additionalProperties }}"
                  title: "{{ title }}"
                title: "{{ title }}"
            defs: "{{ defs }}"
            maxItems: "{{ maxItems }}"
            uniqueItems: {{ uniqueItems }}
            additionalProperties:
              type: "{{ type }}"
              minimum: {{ minimum }}
              items:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              nullable: {{ nullable }}
              maximum: {{ maximum }}
              description: "{{ description }}"
              default: "{{ default }}"
              ref: "{{ ref }}"
              minItems: "{{ minItems }}"
              anyOf:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              enum:
                - "{{ enum }}"
              required:
                - "{{ required }}"
              properties: "{{ properties }}"
              prefixItems:
                - type: "{{ type }}"
                  minimum: {{ minimum }}
                  items:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  nullable: {{ nullable }}
                  maximum: {{ maximum }}
                  description: "{{ description }}"
                  default: "{{ default }}"
                  ref: "{{ ref }}"
                  minItems: "{{ minItems }}"
                  anyOf: "{{ anyOf }}"
                  enum: "{{ enum }}"
                  required: "{{ required }}"
                  properties: "{{ properties }}"
                  prefixItems: "{{ prefixItems }}"
                  defs: "{{ defs }}"
                  maxItems: "{{ maxItems }}"
                  uniqueItems: {{ uniqueItems }}
                  additionalProperties:
                    type: "{{ type }}"
                    minimum: {{ minimum }}
                    items: "{{ items }}"
                    nullable: {{ nullable }}
                    maximum: {{ maximum }}
                    description: "{{ description }}"
                    default: "{{ default }}"
                    ref: "{{ ref }}"
                    minItems: "{{ minItems }}"
                    anyOf: "{{ anyOf }}"
                    enum: "{{ enum }}"
                    required: "{{ required }}"
                    properties: "{{ properties }}"
                    prefixItems: "{{ prefixItems }}"
                    defs: "{{ defs }}"
                    maxItems: "{{ maxItems }}"
                    uniqueItems: {{ uniqueItems }}
                    additionalProperties: "{{ additionalProperties }}"
                    title: "{{ title }}"
                  title: "{{ title }}"
              defs: "{{ defs }}"
              maxItems: "{{ maxItems }}"
              uniqueItems: {{ uniqueItems }}
              additionalProperties:
                type: "{{ type }}"
                minimum: {{ minimum }}
                items: "{{ items }}"
                nullable: {{ nullable }}
                maximum: {{ maximum }}
                description: "{{ description }}"
                default: "{{ default }}"
                ref: "{{ ref }}"
                minItems: "{{ minItems }}"
                anyOf: "{{ anyOf }}"
                enum: "{{ enum }}"
                required: "{{ required }}"
                properties: "{{ properties }}"
                prefixItems: "{{ prefixItems }}"
                defs: "{{ defs }}"
                maxItems: "{{ maxItems }}"
                uniqueItems: {{ uniqueItems }}
                additionalProperties: "{{ additionalProperties }}"
                title: "{{ title }}"
              title: "{{ title }}"
            title: "{{ title }}"
          title: "{{ title }}"
    - name: remoteAgentTool
      description: |
        Optional. The remote agent tool.
      value:
        name: "{{ name }}"
        agentCard:
          version: "{{ version }}"
          name: "{{ name }}"
          description: "{{ description }}"
          skills:
            - name: "{{ name }}"
              tags: "{{ tags }}"
              examples: "{{ examples }}"
              inputModes: "{{ inputModes }}"
              id: "{{ id }}"
              description: "{{ description }}"
              outputModes: "{{ outputModes }}"
          supportedInterfaces:
            - url: "{{ url }}"
              tenant: "{{ tenant }}"
              protocolVersion: "{{ protocolVersion }}"
              protocolBinding: "{{ protocolBinding }}"
        description: "{{ description }}"
    - name: dataStoreTool
      description: |
        Optional. The data store tool.
      value:
        engineSource:
          dataStoreSources:
            - filter: "{{ filter }}"
              dataStore:
                documentProcessingMode: "{{ documentProcessingMode }}"
                createTime: "{{ createTime }}"
                type: "{{ type }}"
                connectorConfig:
                  collection: "{{ collection }}"
                  collectionDisplayName: "{{ collectionDisplayName }}"
                  dataSource: "{{ dataSource }}"
                name: "{{ name }}"
                displayName: "{{ displayName }}"
          engine: "{{ engine }}"
          filter: "{{ filter }}"
        description: "{{ description }}"
        filterParameterBehavior: "{{ filterParameterBehavior }}"
        boostSpecs:
          - spec: "{{ spec }}"
            dataStores: "{{ dataStores }}"
        modalityConfigs:
          - modalityType: "{{ modalityType }}"
            groundingConfig:
              groundingLevel: {{ groundingLevel }}
              disabled: {{ disabled }}
            rewriterConfig:
              disabled: {{ disabled }}
              modelSettings:
                model: "{{ model }}"
                temperature: {{ temperature }}
              prompt: "{{ prompt }}"
            summarizationConfig:
              modelSettings:
                model: "{{ model }}"
                temperature: {{ temperature }}
              prompt: "{{ prompt }}"
              disabled: {{ disabled }}
        dataStoreSource:
          filter: "{{ filter }}"
          dataStore:
            documentProcessingMode: "{{ documentProcessingMode }}"
            createTime: "{{ createTime }}"
            type: "{{ type }}"
            connectorConfig:
              collection: "{{ collection }}"
              collectionDisplayName: "{{ collectionDisplayName }}"
              dataSource: "{{ dataSource }}"
            name: "{{ name }}"
            displayName: "{{ displayName }}"
        name: "{{ name }}"
    - name: etag
      value: "{{ etag }}"
      description: |
        Etag used to ensure the object hasn't changed during a read-modify-write operation. If the etag is empty, the update will overwrite any concurrent changes.
    - name: executionType
      value: "{{ executionType }}"
      description: |
        Optional. The execution type of the tool.
      valid_values: ['EXECUTION_TYPE_UNSPECIFIED', 'SYNCHRONOUS', 'ASYNCHRONOUS']
    - name: agentTool
      description: |
        Optional. The agent tool.
      value:
        name: "{{ name }}"
        agent: "{{ agent }}"
        description: "{{ description }}"
    - name: toolFakeConfig
      description: |
        Optional. Configuration for tool behavior in fake mode.
      value:
        codeBlock:
          pythonCode: "{{ pythonCode }}"
        enableFakeMode: {{ enableFakeMode }}
    - name: toolId
      value: "{{ toolId }}"
`}</CodeBlock>

</TabItem>
</Tabs>


## `UPDATE` examples

<Tabs
    defaultValue="patch"
    values={[
        { label: 'patch', value: 'patch' }
    ]}
>
<TabItem value="patch">

Updates the specified tool.

```sql
UPDATE google.ces.tools
SET 
data__connectorTool = '{{ connectorTool }}',
data__timeout = '{{ timeout }}',
data__name = '{{ name }}',
data__openApiTool = '{{ openApiTool }}',
data__fileSearchTool = '{{ fileSearchTool }}',
data__systemTool = '{{ systemTool }}',
data__widgetTool = '{{ widgetTool }}',
data__pythonFunction = '{{ pythonFunction }}',
data__mcpTool = '{{ mcpTool }}',
data__googleSearchTool = '{{ googleSearchTool }}',
data__clientFunction = '{{ clientFunction }}',
data__remoteAgentTool = '{{ remoteAgentTool }}',
data__dataStoreTool = '{{ dataStoreTool }}',
data__etag = '{{ etag }}',
data__executionType = '{{ executionType }}',
data__agentTool = '{{ agentTool }}',
data__toolFakeConfig = '{{ toolFakeConfig }}'
WHERE 
projectsId = '{{ projectsId }}' --required
AND locationsId = '{{ locationsId }}' --required
AND appsId = '{{ appsId }}' --required
AND toolsId = '{{ toolsId }}' --required
AND updateMask = '{{ updateMask}}'
RETURNING
name,
agentTool,
clientFunction,
connectorTool,
createTime,
dataStoreTool,
displayName,
etag,
executionType,
fileSearchTool,
generatedSummary,
googleSearchTool,
mcpTool,
openApiTool,
pythonFunction,
remoteAgentTool,
systemTool,
timeout,
toolFakeConfig,
updateTime,
widgetTool;
```
</TabItem>
</Tabs>


## `DELETE` examples

<Tabs
    defaultValue="delete"
    values={[
        { label: 'delete', value: 'delete' }
    ]}
>
<TabItem value="delete">

Deletes the specified tool.

```sql
DELETE FROM google.ces.tools
WHERE projectsId = '{{ projectsId }}' --required
AND locationsId = '{{ locationsId }}' --required
AND appsId = '{{ appsId }}' --required
AND toolsId = '{{ toolsId }}' --required
AND force = '{{ force }}'
AND etag = '{{ etag }}'
;
```
</TabItem>
</Tabs>
